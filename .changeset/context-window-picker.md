---
"openai-oauth-copilot-chat": minor
---

Adds a Context Window picker control (Auto, 64K, 128K, 200K, Maximum) that caps how much conversation history each Codex request sends, clamped to the model's registered input limit.