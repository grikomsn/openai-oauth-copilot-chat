---
"openai-oauth-copilot-chat": minor
---

Discover models from the signed-in account's live Codex catalog, use its requestable reasoning efforts and default without a local allowlist or workspace override, exclude the internal-only `ultra` delegation level, and expose models advertising the Fast additional speed tier as separate entries.
